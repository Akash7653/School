# Frontend environment variables (example)
# Copy to `.env` in the frontend folder and restart dev server

# Base backend URL (omit trailing slash)
REACT_APP_BACKEND_URL=http://localhost:8001

# Example: enable analytics in dev
REACT_APP_ENABLE_ANALYTICS=false
